//
// Modules.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Various modules returned for rendering dashboard. */
public struct Modules: Codable, Hashable {

    /** Key of the chart. */
    public var key: String
    /** Label for the chart. */
    public var label: String
    /** Type of chart to insert te data into. */
    public var moduleType: String
    /** Order at which the chart appears on the web page. */
    public var order: String
    /** Array of DataSets map reference keys. If style information is required for a specific chart type then it will be returned. */
    public var dataSets: [DataSetsReference]

    public init(key: String, label: String, moduleType: String, order: String, dataSets: [DataSetsReference]) {
        self.key = key
        self.label = label
        self.moduleType = moduleType
        self.order = order
        self.dataSets = dataSets
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case key
        case label
        case moduleType
        case order
        case dataSets
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(key, forKey: .key)
        try container.encode(label, forKey: .label)
        try container.encode(moduleType, forKey: .moduleType)
        try container.encode(order, forKey: .order)
        try container.encode(dataSets, forKey: .dataSets)
    }
}

